# Pinned for Vercel's @vercel/python runtime (Python 3.12).
# Uvicorn intentionally absent — Vercel wraps the ASGI app itself.
fastapi>=0.115
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12
python-docx>=1.1
httpx>=0.27
pillow>=11.0
